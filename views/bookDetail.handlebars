<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{book.Bname}}</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <section class="book-details">
        <div class="leftSide">        
            <h1>{{book.Bname}}</h1>
            <img src="{{book.Bimage}}" alt="{{book.Bname}}">
        </div>
        <div class="rightSide">
            <h3>Type: <p>{{book.Btype}}</p></h3>
            <h3>Description: <p>{{book.Bdesc}}</p></h3>
            <h3>Year: <p>{{book.Byear}}</p></h3>
        </div>
    </section>
</body>
</html>
